function newTodo() {
    /*
    // get the title
    var title = document.getElementById("title");
    var badge = document.getElementById("badge");
    var alertpanel = document.getElementById("alertpanel");
    var counter = parseInt(badge.innerText);
    var ul = document.getElementById ("todolist_ul");
    // create a new <li>title</li>
    var li = document.createElement("li");
    li.innerText = title.value;
    badge.innerText = (++counter).toString();
    // add the <li> to the <ul>
    alertpanel.innerHTML = '<div class="alert alert-info"><button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button><strong>Information!</strong> A new todo has been added!</div>';
    ul.appendChild(li);
}

function setDetails() {
    var li = window.event.target;
    var title = document.getElementById("detail_title");
    //var descr = document.getElementById("detail_descr"); 
    title.innerText = li.innerText;
}
*/
}



